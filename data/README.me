# Contains all the scraped data.

USE `rf_data.csv`. It is the most current of the data files and contains columns "title", "year", "genre", "country", and "url". This file is a combination of `cleaned_titles_data.csv` and `cleaned_urls.csv`.
